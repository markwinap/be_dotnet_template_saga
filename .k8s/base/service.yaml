apiVersion: v1
kind: Service
metadata:
  name: order-service-api
  namespace: order-service
spec:
  selector:
    app: order-service-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
